const express = require('express');
const router = express.Router();

// Placeholder for battle routes
router.get('/', (req, res) => {
  res.json({ message: 'Colts Game battles coming soon' });
});

module.exports = router;